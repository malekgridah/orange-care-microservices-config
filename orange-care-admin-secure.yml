spring:
  security:
    user:
      name: "ADMX"
      password: "ADMX"
  boot:
    admin:
      client:
        username: "ADMX"       #These two are needed so that the client
        password: "ADMX"   #can register at the protected server api
        instance:
          metadata:
            user.name: "ADMX"         #These two are needed so that the server
            user.password: "ADMX" #can access the protected client endpoints

info.tags.security: secured
